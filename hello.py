# hello world..again

import pgzrun   # imports zero asset
def draw():     # function for drawing on screen
    screen.draw.text("Hello World",topleft=(10, 10))
    # the above creats the text, easy to follow,
    # givving the text 1st in "" and then instructions to place it
pgzrun.go()